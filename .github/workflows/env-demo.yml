name: env demo
on:
    workflow_dispatch:
env:
    MY_ENV: dev
jobs:
    env-demo:
        env:
            MY_APP: TEST
        runs-on: ubuntu-latest
        steps:
            - name: deploying env
              env:
                MY_ENV: PROD
              run: echo "App is connecting to $MY_ENV"
