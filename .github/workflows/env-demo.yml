name: env demo
on:
    workflow_dispatch:
env:
    MY_ENV: dev
jobs:
    env-demo:
        runs-on: ubuntu-latest
        steps:
            - name: deploying env
              run: echo "App is connecting to {{ $MY_ENV }}"
